---
server:
  disable: true
clients:
  -
    url: https://loki.thgttg.com/api/v1/push
positions:
  filename: /tmp/positions.yml
scrape_configs:
  -
    job_name: journal
    journal:
      max_age: 12h
      json: false
      labels:
        job: journal
    relabel_configs:
      -
        action: keep
        source_labels:
          - __journal__systemd_unit
        regex: (dendreth-commitment-mapper).service
      -
        source_labels:
          - __journal__systemd_unit
        target_label: unit
